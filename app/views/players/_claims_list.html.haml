.row
  .col-md-12
    %h1 Заявки от игроков
.row
  .col-md-12
    - game.potential_participants.each do |participant|
      .row
        .col-md-4= participant.name
        .col-md-4= button_to 'Подтвердить участие', approve_participant_game_path(id: game.id, player_id: participant.id), class: 'btn btn-default'
        .col-md-4= button_to 'Отклонить заявку', deny_participant_game_path(id: game.id, player_id: participant.id), class: 'btn btn-default'
